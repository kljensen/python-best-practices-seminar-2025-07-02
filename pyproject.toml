[project]
name = "seminar"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.2.1",
    "httpx>=0.28.1",
    "loguru>=0.7.3",
    "mypy>=1.16.1",
    "pyquery>=2.0.1",
]
